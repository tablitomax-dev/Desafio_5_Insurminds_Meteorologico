# Manifest de desenvolvimento — gate obrigatório de CI
# (intent 001-repository-quality-foundation, bolt 001-pre-merge-quality-repair).
#
# A maioria do app/ é stdlib-only. Exceção: o modo LLM da demo (story 06,
# app/adapters/llm_messages.py) depende de pydantic-ai — import LAZY, então
# o modo template (default, sem env) nunca exige o SDK. Pinned idêntico ao
# .venv local para CI/dev executarem o MESMO ambiente (ruff 0.16.5, mypy 2.3.1,
# pytest 9.1.1, pydantic-ai 1.107.1).

ruff==0.16.5
mypy==2.3.1
pytest==9.1.1
pydantic-ai==1.107.1
